spring:
  cloud:
    config: #Config客户端配置
      uri: http://192.168.56.1/config-server #配置中心地址
      profile: dev #启用配置后缀名称
      label: demo #分支名称
      name: demo #配置文件名称

management:
  endpoints:
    web:
      exposure:
        include: 'refresh'